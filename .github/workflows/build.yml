name: Build

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  build:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: ["ubuntu-latest", "macos-latest", "windows-latest"]
        python-version: ["3.10", "3.11", "3.12", "3.13"]

    steps:
    - uses: actions/checkout@v4

    - name: Set up Miniconda
      uses: conda-incubator/setup-miniconda@v3
      with:
        python-version: ${{ matrix.python-version }}
        auto-update-conda: true
        channels: conda-forge
        activate-environment: aidrin

    - name: Ensure pip in conda env
      shell: bash -l {0}
      run: |
        conda install -n aidrin -c conda-forge pip -y

    - name: Install package (editable)
      shell: bash -l {0}
      run: |
        python --version
        which python
        python -m pip install -e .

    - name: Build sdist and wheel
      shell: bash -l {0}
      # --no-isolation: build in the conda env with the pinned backend instead
      # of an isolated venv. `python -m build`'s venv can't find conda's
      # libexpat DLL on Windows (pyexpat ImportError), so build without it and
      # pre-install the exact build-system.requires ourselves.
      run: |
        python -m pip install build setuptools==78.1.1 wheel
        python -m build --no-isolation

    - name: Verify wheel installs
      shell: bash -l {0}
      run: |
        python -m pip install dist/*.whl
        python -c "import aidrin; print('aidrin', aidrin.__version__)"
        python -c "import web; print('web package OK')"
